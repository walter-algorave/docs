---
title: "Esecuzione dei Test"
description: "Come eseguire la suite di test con pytest, opzioni utili e interpretazione dell'output."
icon: "play"
---

## Prerequisiti

I test richiedono le stesse dipendenze del progetto principale (vedi [Installazione](/installazione)), più pytest:

```bash
pip install pytest
```

`test_tools_image.py` richiede inoltre `opencv-python` e `numpy` (già inclusi in `requirements.txt`).

---

## Esecuzione Base

```bash
# Dalla root del progetto
pytest tests/
```

Output atteso con tutti i test passanti:

```
========================= test session starts =========================
platform win32 -- Python 3.13.x, pytest-9.x.x
collected 277 items

tests/test_models.py .................. [ 6%]
tests/test_settings.py ........................... [ 16%]
tests/test_engine.py ............................  [ 38%]
tests/test_decision_engine.py ................................... [ 51%]
tests/test_postflop_engine.py ........................ [ 60%]
tests/test_hand_eval.py .................  ...........[ 78%]
tests/test_tools_image.py ................................. [ 90%]
tests/test_integration.py ......... [ 93%]
tests/test_file_logger.py .......... [ 97%]
tests/test_gui_output.py ............ [100%]

========================= 277 passed in X.XXs =========================
```

---

## Opzioni Utili

### Verboso (mostra ogni test)

```bash
pytest tests/ -v
```

### Solo un file

```bash
pytest tests/test_decision_engine.py
```

### Solo un caso di test

```bash
pytest tests/test_decision_engine.py::TestHandKey
pytest tests/test_decision_engine.py::TestHandKey::test_suited
```

### Ferma al primo errore

```bash
pytest tests/ -x
```

### Mostra output stdout (print)

```bash
pytest tests/ -s
```

### Report di copertura

```bash
pip install pytest-cov
pytest tests/ --cov=app --cov-report=term-missing
```

---

## Test che Richiedono Attenzione

<AccordionGroup>
  <Accordion title="test_settings.py — TESSERACT_PATH" icon="circle-info">
    Il test verifica che `TESSERACT_PATH` sia una stringa valida, **non** che il file esista. Questo test passa anche se Tesseract non è installato.
    
    Per verificare che Tesseract sia funzionante, usa:
    ```bash
    pytest tests/test_tools_image.py -k "ocr"
    ```
  </Accordion>
  <Accordion title="test_tools_image.py — Template PNG" icon="circle-info">
    Alcuni test usano immagini sintetiche generate con numpy. Non richiedono i template card reali in `templates/cards/`. Il test di loading dei template (`test_load_card_templates`) verifica solo la struttura del dict restituito, non il contenuto dei PNG.
  </Accordion>
  <Accordion title="test_engine.py — Nessuna finestra PokerTH richiesta" icon="circle-check">
    Tutti i test di `GameSession` e `StateTracker` usano screenshot mock. Non serve PokerTH aperto. Le dipendenze Win32 sono bypassate tramite injection.
  </Accordion>
</AccordionGroup>

---

## Struttura delle Classi di Test

```
tests/
├── helpers.py                  ← make_snapshot(), make_card(), calibrate_tracker()
├── conftest.py                 ← sys.path + fixture pytest
├── test_models.py
│   ├── TestColors
│   ├── TestCard
│   └── TestScreenshot
├── test_settings.py
│   ├── TestSettingsStructure
│   ├── TestSettingsPositions
│   ├── TestSettingsColorRanges
│   └── TestSettingsCriticalThresholds
├── test_engine.py
│   ├── TestCardReaderInstanceCache
│   ├── TestStateTrackerObserver
│   ├── TestConsoleOutput
│   ├── TestGameSessionState
│   ├── TestGameSessionFoldLogic
│   ├── TestHandHistoryTracker
│   ├── TestIsHeroFolded
│   ├── TestIsHeroTurn
│   ├── TestGameSessionHandId
│   └── TestAnalysisEngineThreadSafety
├── test_decision_engine.py
│   ├── TestHandKey
│   ├── TestLookup
│   ├── TestSeatToPosition
│   ├── TestNormalizePosition
│   └── TestPreflopDecisionEngine
├── test_postflop_engine.py
│   ├── TestPostflopGuards
│   ├── TestFlopDecision
│   ├── TestTurnDecision
│   └── TestRiverDecision
├── test_hand_eval.py
│   ├── TestBoardTexture
│   ├── TestHasTopPair
│   ├── TestEvaluateFive
│   ├── TestBestHandWithHero
│   └── TestPlayerRole
├── test_tools_image.py
│   ├── TestEmptyCard
│   ├── TestColorDetection
│   ├── TestGetCardColor
│   ├── TestGetRelativePosition
│   ├── TestGetPlayerPosition
│   ├── TestCardReaderFiltration
│   ├── TestCardReaderHiddenDebounce
│   └── TestAnalyzeHeroRoi
├── test_integration.py
│   ├── TestColdStartMidflopNewHandPostflopSuggestion
│   ├── TestFullStreetSequence
│   ├── TestCompromisedHandReset
│   └── TestBBInferenceAcrossSnapshots
├── test_file_logger.py
│   ├── TestFileLoggerWritesJsonl
│   ├── TestFileLoggerSessionIsolation
│   └── TestFileLoggerRobustness
└── test_gui_output.py
    └── TestGuiOutput
```

---

## Aggiungere Nuovi Test

Il pattern di riferimento usa `make_snapshot()` da `helpers.py`:

```python
# helpers.py è importabile direttamente (non conftest)
from helpers import make_snapshot, make_card, calibrate_tracker

class TestMyScenario(unittest.TestCase):
    def test_example(self):
        tracker = HandHistoryTracker()
        engine  = PreflopDecisionEngine(tracker)
        calibrate_tracker(tracker)          # hand_id 0→Flop, 1→Preflop
        snapshot = make_snapshot(
            position="EP",
            bets={1: 0.5, 2: 1.0},
            cards=[make_card("A"), make_card("K")]
        )
        tracker.update(snapshot)
        self.assertEqual(engine.evaluate(snapshot), Decision.RAISE)
```

Per test di integrazione (zero mock), usare `DecisionRouter` direttamente:

```python
from helpers import make_snapshot, make_card as c, calibrate_tracker
from engine.hand_history import HandHistoryTracker
from engine.decision_router import DecisionRouter
from engine.decision_engine import Decision

def test_full_flow():
    tracker = HandHistoryTracker()
    router  = DecisionRouter(tracker)
    calibrate_tracker(tracker)
    snap = make_snapshot(game_state="Flop", hand_id=1,
                         cards=[c("K"), c("3")],
                         table_cards=[c("K"), c("7"), c("2")])
    tracker.update(snap)
    assert router.evaluate(snap) == Decision.BET_75
```
